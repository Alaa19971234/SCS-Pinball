extends StaticBody3D


func _ready():
	# Get the static body node
	#var Board = $"collisions&meshes/Board"
	
	# Get the material
	#var material = Board.mesh.material

	# Set the color of the material
	#Board.mesh.material.albedo_color = Color(1, 0, 0, 1) # Red color
	pass
	
#func _process(delta):
	#if GlobalScore.score <= 100:
		
	#elif GlobalScore.score >= 100 and GlobalScore.score <= 1000:

			
	#elif GlobalScore.score >= 1000 and GlobalScore.score <= 10000:
	#pass

